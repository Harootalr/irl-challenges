# IRL Challenges - Environment Variables Configuration
# Copy this file to .env and fill in your actual values

# Database Configuration
# PostgreSQL connection string - required for data persistence
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# Authentication & Security
# JWT secret for signing authentication tokens - MUST be a strong random string (32+ characters)
JWT_SECRET=your_super_secure_jwt_secret_key_here_minimum_32_characters

# Server Configuration
# Server port (defaults to 5000 if not set)
PORT=5000

# Environment - affects security settings (development/production)
NODE_ENV=development

# WebSocket Security
# Allowed origin for WebSocket connections (your frontend domain)
ALLOWED_ORIGIN=https://yourapp.replit.app

# CORS Configuration (Production)
# Comma-separated list of allowed origins for cross-origin requests
# Example: ALLOWED_ORIGINS=https://yourapp.replit.app,https://yourdomain.com
# Leave empty or unset to block all cross-origin requests in production
ALLOWED_ORIGINS=https://yourapp.replit.app

# Frontend Environment Variables (VITE_ prefix required for client-side access)
# Google Maps API key for venue location features (if using maps)
# VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Replit-specific variables (automatically provided by Replit)
# REPL_ID=your_repl_id
# REPLIT_DOMAIN=your_repl_domain

# Session & Cookie Security (Production)
# Set cookie domain for production deployments (optional - auto-detects if not set)
# COOKIE_DOMAIN=yourdomain.com

# Production Deployment Notes:
# 1. Use strong random values for JWT_SECRET  
# 2. Set NODE_ENV=production for production deployments
# 3. Update ALLOWED_ORIGIN to match your production domain
# 4. Ensure DATABASE_URL points to your production database
# 5. Set COOKIE_DOMAIN for cookie security (if needed)
# 6. Never commit actual secrets to version control